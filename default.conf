server {
    listen 80 default_server;
    index index.php index.html index.htm;
    server_name localhost; ## local에서 할 때 (도메인네임 없이)
    #server_name qspblog.com www.qspblog.com;   ## server에서 적용시 바꾸기 위를 주석처리하기
    error_log /var/log/nginx/error.log;
    access_log /var/log/nginx/access.log;
    root /var/www/html/public;
    ## root /app/public; # 라라벨 없이 php만으로 서비스 할 때 - 실제로 로컬 프로젝트디렉토리에 /app/public/index.php 로 테스트
    ## /var/www/html/public 까지 올려주면 라라벨이 작동 - laravel로 새로운 프로젝트를 app 로 최상위 디렉토리에 만듬

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        try_files $uri $uri/ /index.php?$query_string;
        #try_files $uri $uri/ https://example.org/index.php?$query_string;  ## need to be changed
    }

    location ~ \.php$ {
        try_files $uri =404;
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        fastcgi_pass php:9000;
        fastcgi_index index.php;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param PATH_INFO $fastcgi_path_info;
    }
}

## 서버에서 적용시 주석해제
# server {
#     listen 443 default_server ssl http2;
#     listen [::]:443 ssl http2;

#     server_name qspblog.com; ## your server name
    
#     ssl_certificate /etc/nginx/ssl/live/example.org/fullchain.pem;  ## need to change from example.org to my domain name
#     ssl_certificate_key /etc/nginx/ssl/live/example.org/privkey.pem;
    
#     location / {
#         try_files $uri $uri/ /index.php?$query_string;
#     }

#     location ~ \.php$ {
#         try_files $uri =404;
#         fastcgi_split_path_info ^(.+\.php)(/.+)$;
#         fastcgi_pass php:9000;
#         fastcgi_index index.php;
#         include fastcgi_params;
#         fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
#         fastcgi_param PATH_INFO $fastcgi_path_info;
#     }
# }